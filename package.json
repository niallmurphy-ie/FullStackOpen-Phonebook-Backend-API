{
  "name": "phonebook-api",
  "version": "1.0.0",
  "description": "API for React Phonebook App",
  "main": "index.js",
  "scripts": {
    "build:ui": "rm -rf build && cd ../FullStackOpen/part2/phonebook/ && npm run build --prod && cp -r build ../../../\"FullStackOpen Phonebook Backend\"",
    "deploy": "git push heroku main",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push && npm run deploy",
    "logs:prod": "heroku logs --tail",
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^7.32.0",
    "eslint-config-airbnb-base": "^14.2.1",
    "eslint-plugin-import": "^2.24.2",
    "nodemon": "^2.0.12"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "heroku": "^7.59.0",
    "mongoose": "^5.13.8",
    "mongoose-unique-validator": "^2.0.3",
    "morgan": "^1.10.0"
  }
}
